# ============================================
# Vehicle Management System - Backend Environment Variables
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file and rename it to ".env"
# 2. Replace the example values with your actual credentials
# 3. NEVER commit the actual .env file to version control
# ============================================

# ============================================
# Server Configuration
# ============================================
# Port number for the backend server to run on
PORT=4000

# ============================================
# Database Configuration
# ============================================
# MongoDB connection string
# Format: mongodb://localhost:27017/database-name (for local)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database-name (for MongoDB Atlas)
MONGO_URI=mongodb://localhost:27017/vehicleManagementDB

# ============================================
# Authentication & Security
# ============================================
# Secret key for JWT token generation and verification
# Generate a strong random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# Optional Configuration
# ============================================
# Node environment (development, production, test)
NODE_ENV=development

# Session secret (if using sessions)
# SESSION_SECRET=your-session-secret-key

# CORS allowed origins (comma separated for multiple origins)
# CORS_ORIGIN=http://localhost:3000

# Maximum file upload size (in MB)
# MAX_FILE_SIZE=10

# ============================================
# Email Configuration (if needed for notifications)
# ============================================
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-email-password
# EMAIL_FROM=noreply@vehiclemanagement.com

# ============================================
# Cloud Storage (if migrating from local uploads)
# ============================================
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_BUCKET_NAME=your-s3-bucket-name
# AWS_REGION=us-east-1
